{% extends "layout.twig" %}

{# This content is rendered under the `content` block in `layout.html` #}
{% block body %}
    {% if currentUser %}
        {% include "partials/dashboard.twig" %}
    {% else %}
        {% include "partials/home.twig" %}
    {% endif %}
{% endblock %}